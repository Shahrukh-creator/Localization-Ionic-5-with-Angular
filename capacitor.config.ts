import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic5_Translation',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
